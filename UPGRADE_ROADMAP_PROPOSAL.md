# PhyzBox 后续升级路线提案（已批准，首个里程碑完成）

日期：2026-08-04
文档状态：**已批准；专业物理库与轨道游戏 MVP 里程碑已完成**
执行规则：本文件只提出方案；未经项目所有者明确批准，不实施其中任何功能改动。

## 一、先确定项目定位

PhyzBox 目前同时包含三种产品倾向：

| 方向 | 核心价值 | 优点 | 风险 |
|---|---|---|---|
| A. 天体物理教学实验室 | 直观展示轨道、守恒量、混沌与交会 | 最符合现有实时 3D、HUD 和场景基础；用户容易理解 | 需要把分析工具做扎实，不能只追求画面 |
| B. 轻量科研级 N-body 引擎 | 可验证、可复现、可扩展的数值求解 | 技术含量和可信度最高，可形成真正引擎架构 | 数值方法、测试和文档工作量大 |
| C. 太空驾驶/探索沙盘 | 飞船操控、接近行星和视觉体验 | 交互性强，容易展示 | 最容易再次滑向预设玩法，稀释物理引擎定位 |

### 推荐定位

建议选择 **A + B：交互式天体动力学实验室**。

也就是：

- 底层按轻量科研工具的标准做状态、力模型、积分器和验证。
- 上层用实时 3D、交互编辑和图表帮助人理解动力学。
- 飞船操控可以保留，但只是一个“受控外力实验”，不作为项目主线。

一句话定位建议：

> PhyzBox 是一个可交互、可验证、可扩展的实时天体动力学实验室。

## 二、为什么推荐这条路线

成熟工具的共同方向可以作为参照：

- [REBOUND](https://rebound.hanno-rein.de/integrators/) 把积分器作为 N-body 软件的核心，并允许选择或扩展不同数值方法；它同时提供轨道参数、能量误差和可恢复快照。
- [REBOUNDx](https://reboundx.readthedocs.io/en/latest/effects.html) 把广义相对论、潮汐、质量变化等作为额外物理效应组合到基础积分中。
- [Orekit](https://www.orekit.org/site-orekit-development/architecture/orbits.html) 明确区分轨道表示、动力学传播和物理模型，避免一个对象同时承担所有职责。
- [Orekit ForceModel](https://orekit.org/static/apidocs/org/orekit/forces/ForceModel.html) 将牛顿引力、第三体、推力、阻力、辐射压力和相对论等作为可注册的力模型。
- [NASA SPICE](https://naif.jpl.nasa.gov/pub/naif/toolkit_docs/C/req/spk.html) 说明了真实历表、时间系统和参考系在严肃天体软件中的重要性。

PhyzBox 不需要复制这些大型项目，但应学习它们的分层方式。

## 三、推荐升级路线总览

建议分四个阶段推进。每个阶段都能独立形成一个可发布版本，不必一次做完。

| 阶段 | 名称 | 核心结果 | 建议优先级 |
|---|---|---|---|
| 1 | 引擎骨架与可信度 | 力模型、积分器、状态和事件解耦；建立验证基线 | P0，最先做 |
| 2 | 轨道分析工作台 | 轨道根数、参考系、图表、测量工具 | P1，用户价值最高 |
| 3 | 近距离交会与积分器选择 | 固定步长辛积分 + 自适应高精度积分器 | P1，数值能力升级 |
| 4 | 高级物理与大 N | J2、辐射、潮汐、Barnes-Hut、真实历表 | P2，按需求选择 |

## 四、阶段 1：引擎骨架与可信度

### 目标

把当前集中在 `NBodySystem` 内的逻辑拆成清晰、可替换、可测试的模块。这一步不会增加很多视觉效果，但会决定项目以后是否能稳健扩展。

### 建议模块

```text
SimulationState
  ├─ bodies / time / units / reference frame
  └─ deterministic seed and metadata

ForceModel
  ├─ NewtonianGravity
  ├─ PaczynskiWiitaGravity (optional)
  └─ SpacecraftThrust (non-conservative)

Integrator
  ├─ Leapfrog2
  └─ Yoshida4

EventDetector
  ├─ Collision
  ├─ CloseApproach
  ├─ RocheLimit
  └─ Escape / HillSphere

Diagnostics
  ├─ energy / momentum / angular momentum
  ├─ center of mass
  └─ error history
```

### 具体升级项

1. 定义 `IForceModel` 接口，只负责根据系统状态计算加速度或外力。
2. 定义 `IIntegrator` 接口，只负责推进状态，不处理渲染和事件文案。
3. 把碰撞、洛希极限、近距离交会从积分主循环中拆为事件检测器。
4. 区分保守力与非保守力；有飞船推力时，HUD 不再把总能量变化误标为数值误差。
5. 为每次模拟记录：初始条件、单位制、积分器、时间步、软化长度、启用的力模型和随机种子。
6. 增加可恢复的二进制或文本快照，而不仅是导出当前位置 CSV。
7. 建立确定性测试：相同初始状态、配置和 seed 必须得到相同结果。

### 验收标准

- 原有场景在允许误差内保持相同行为。
- 可以不改积分主循环，单独启用/禁用某个力模型。
- Leapfrog2 和 Yoshida4 可以通过同一个接口切换。
- 保守系统测试能量、线动量、角动量；非保守系统单独记录外力做功。
- 快照恢复后继续运行，与不中断运行结果一致。

### 预计影响文件

- 新增 `src/physics/` 或同类目录。
- 重构 `src/NBodySystem.*`。
- 修改 `src/main.cpp` 自检入口。
- 少量调整 `Renderer` 读取诊断数据的方式。

### 风险

- 属于架构重构，表面变化不大但影响范围广。
- 必须先补测试再迁移，不能一次性重写。

## 五、阶段 2：轨道分析工作台

### 目标

让用户不只是“看见球在动”，还可以读懂轨道为什么这样变化。这是最能提升物理引擎辨识度的用户功能。

### 具体升级项

1. 选中任一天体后显示瞬时轨道根数：
   - 半长轴 `a`
   - 偏心率 `e`
   - 轨道倾角 `i`
   - 升交点经度 `Ω`
   - 近心点幅角 `ω`
   - 真近点角 `ν`
2. 支持切换参考中心：
   - 惯性原点
   - 系统质心
   - 指定主天体
3. 显示物理向量：速度、加速度、角动量、偏心率向量。
4. 增加时间序列图：
   - 总能量误差
   - 角动量误差
   - 天体间距
   - 半长轴/偏心率随时间变化
5. 增加测量工具：点击两个天体查看距离、相对速度、闭合速度和预计最近接近时刻。
6. 增加零速度曲线、Hill 球或拉格朗日点显示，作为受限三体实验工具。
7. 支持以轨道根数创建初始条件，而不仅是手填笛卡尔位置与速度。

### 推荐首批实验模板

- 开普勒两体与轨道根数。
- Hohmann 转移。
- 行星引力弹弓前后能量变化。
- 地月式受限三体与 L1–L5。
- Kozai–Lidov 振荡。
- 三体混沌与 Lyapunov 发散。
- 水星式近心点进动对比：牛顿 / 伪牛顿。

这些应是“初始条件模板”，不是预设动画；启动后仍由同一物理核心演化。

### 验收标准

- 两体椭圆轨道算出的 `a/e/i` 与初始设定一致。
- 参考中心切换不改变物理状态，只改变展示与分析坐标。
- 图表数据可以导出 CSV。
- 至少三个经典问题能与解析解或公开参考结果比较。

### 风险

- 经典轨道根数在圆轨道、赤道轨道附近存在奇异性；内部最好同时支持非奇异表示或明确显示退化状态。
- 当前 Win32/OpenGL 固定管线 UI 较原始，复杂图表可能需要自绘组件。

## 六、阶段 3：近距离交会与积分器选择

### 目标

不同物理问题使用适合的数值积分器，而不是所有情况只用一套方法。

### 推荐设计

| 模式 | 适用问题 | 特点 |
|---|---|---|
| Yoshida4 固定步长 | 长时间、近开普勒、保守系统 | 长期能量性质好，速度快 |
| Leapfrog2 固定步长 | 教学对照、快速预览 | 简单、稳健、便于解释 |
| Dormand-Prince 5(4) | 一般高精度、非保守力 | 自适应误差控制，实现成本适中 |
| IAS15 类高阶积分器 | 极高精度、强近交会 | 能力强，但实现和验证成本最高 |

建议先实现 Dormand-Prince 5(4)，不要直接挑战完整 IAS15。

### 具体升级项

1. GUI 与配置文件可选择积分器。
2. 固定步长辛模式不再使用状态相关的连续变步长，避免破坏辛结构。
3. 近距离交会时可选择：
   - 提示用户切换自适应积分器；或
   - 自动切换到近交会积分器并记录切换事件。
4. 自适应模式公开绝对/相对误差容限、拒绝步数和实际步长。
5. 同一场景可以并排比较两个积分器的轨迹和误差。

### 验收标准

- 两体轨道运行 1,000 周期后比较相位误差与能量误差。
- 高偏心轨道在近心点不会数值爆炸。
- 近距离双曲飞掠结果随误差容限收敛。
- HUD 清楚显示当前积分器、固定/自适应模式和实际步长。

### 风险

- “Yoshida + 自适应细分”需要重新定义，不能继续模糊地宣称严格辛积分。
- 自动积分器切换会引入状态连续性和误差基准问题，需要专项测试。

## 七、阶段 4：高级物理与大 N（按需选择）

这一阶段不建议全部同时做，应根据项目定位选择子路线。

### 4A. 可组合摄动力

- 非球形引力 `J2/J3/J4`。
- 第三体摄动。
- 辐射压力。
- Poynting–Robertson 阻力。
- 连续/脉冲推力与燃料质量变化。
- 1PN 相对论修正。
- 恒定时滞潮汐与自旋耦合。

原则：每个模型必须附公式来源、适用范围、单位测试和基准案例。

### 4B. 大 N 性能

- Barnes-Hut 八叉树，目标从几十体扩展到数千或数万体。
- 活跃/无质量粒子分组，避免测试粒子反向影响主系统。
- 多线程成对力计算。
- 性能 HUD：力评估次数、步耗时、树误差参数。

建议顺序：先多线程 direct N-body，再 Barnes-Hut；这样更容易验证两者误差。

### 4C. 碰撞与碎裂

- 完全非弹性合并之外，增加弹性系数和切向摩擦。
- 按碰撞能量区分并合、掠碰、侵蚀和灾变碎裂。
- 碎片成为有质量体，或明确标记为无质量可视化粒子。
- SPH 暂不建议做，除非项目明确转向流体天体物理。

### 4D. 真实太阳系数据

- 读取 JPL/NAIF SPICE 星历和参考系。
- 支持 UTC、TT、TDB 等时间系统的明确转换。
- 加入“真实太阳系某一历元”模板。
- 模拟结果与星历差异可视化。

这一项会打破“零外部依赖”的纯粹性，可采用可选模块而不是强制依赖。

## 八、我最推荐的下一版：v1.1 物理工作台

如果只选一批升级，我建议下一版不要做 Barnes-Hut、更多黑洞特效或地表玩法，而是完成下面这组：

### v1.1 范围

1. `ForceModel` 与 `Integrator` 最小接口。
2. 固定步长 Yoshida4 / Leapfrog2 切换。
3. 保守力与外力做功分开诊断。
4. 选中天体显示 `a/e/i` 和相对主天体状态。
5. 能量、角动量、距离随时间曲线。
6. 快照保存/恢复，并记录完整模拟元数据。
7. 两体、Hohmann、引力弹弓、受限三体四个可验证实验模板。

### 为什么是这组

- 同时增强底层可信度和用户可见价值。
- 不需要立即引入大型第三方库。
- 每一项都能用明确数值标准验收。
- 完成后，项目打开时首先呈现的是“实验与测量”，而不是“驾驶和关卡”。

### 暂不纳入 v1.1

- 数百颗活动行星。
- 程序化地表、资源和任务系统。
- SPH 流体。
- 完整数值广义相对论。
- 网络联机。
- 复杂飞船建造系统。
- 未经验证的视觉特效型“新物理”。

## 九、建议开发顺序

### 里程碑 1：基准冻结

- 保存当前所有测试输出和典型轨迹。
- 增加两体解析解、圆轨道周期和双曲散射基准。
- 定义允许误差。

### 里程碑 2：最小接口迁移

- 先抽出 Newtonian force。
- 再抽出 Leapfrog/Yoshida。
- 保证每迁移一步都通过旧测试。

### 里程碑 3：分析数据层

- 实现参考中心和瞬时轨道根数。
- 建立不依赖 OpenGL 的时间序列缓存。

### 里程碑 4：可视化与交互

- HUD 选中体面板。
- 诊断曲线。
- 实验模板选择。

### 里程碑 5：保存、恢复与发布验收

- 快照回放一致性。
- 文档、示例和误差报告。
- GUI 人工验收和无窗口回归测试。

## 十、需要你审阅的决策

请重点决定以下事项：

1. 是否接受“交互式天体动力学实验室”作为主定位。
2. 是否同意 v1.1 先做引擎接口、轨道分析、图表和快照，不做更多游戏玩法。
3. 飞船操控是保留为次要实验，还是完全移出主场景。
4. 是否必须继续坚持零外部依赖。
5. 是否希望下一份方案进一步细化为：
   - 技术架构设计；
   - UI 原型；
   - 分阶段文件修改清单；
   - 测试矩阵与验收指标。

## 十一、审批栏

- [ ] 同意推荐定位
- [ ] 同意 v1.1 范围
- [ ] 需要调整范围
- [ ] 暂不实施

审批备注：

> 等待项目所有者审阅。
