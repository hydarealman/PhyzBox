# PhyzBox 物理库与游戏前端实施报告

日期：2026-08-11
状态：已完成物理库、旅行者历史太阳系垂直切片、真实星空、极简时间控制、自动测试与 Windows 构建链。

## 2026-08-11：旅行者太阳系重构

默认游戏入口已从《余烬航线》文字驾驶舱切换到旅行者 1 号历史航行。此次重构删除了原有任务卡、下拉框、大段遥测文本和时间轴；玩家通过紧凑时间条与键鼠直接控制历史播放、时间倍率、事件跳转、镜头和太阳系总览。

后续可用性验收发现，仅依赖 `+ / -` 和窗口标题调整时间不够直观，因此恢复了唯一的紧凑屏幕控件：底部九档时间流速拖动条。它持续显示当前倍率，并与播放按钮、`Space`、`+ / -` 双向同步；没有恢复任务卡、大段文字或遥测面板。

拖动条初版在历史事件附近仍会被自动减速限制，导致玩家选择高速档后实际只有 `1分钟/秒`。现已改为任何玩家主动拖动或 `+ / -` 调速都会进入手动模式，并明确显示“手动”；按 `A` 可重新启用自动飞掠保护。回归测试直接验证在发射事件时选择 `1小时/秒`，运行两秒必须推进 7,200 秒。

历史状态来自 JPL NAIF 的 DE440s、Voyager 1 和 Voyager 2 SPK 内核。导入器把 1977–2030 状态转换为可审计的 `PHYZEPH2` 二进制数据；运行时保留双精度，在采样间执行位置 / 速度一致的三次 Hermite 插值。飞掠窗口使用小时级采样，其余阶段使用日 / 周级采样。

视觉层新增：

- 83,337 颗 Hipparcos 恒星，位置、V 视星等和 B−V 色指数来自 NASA HEASARC 的 Hipparcos 表。
- 按 J2000 银河北极 / 银河中心方向渲染的程序化银河散射和尘埃带，不使用 AI 图片或全景位图。
- 相机局部双尺度渲染：物理值保持 km / km·s⁻¹，近景保持真实角直径，太阳系总览使用 AU。
- 程序化旅行者部件模型、行星表面、云层与大气边缘光。
- 地球起点构图按真实约 7,787 km 地心距离显示，巨大地球自然越过画面边缘。
- 默认“任务可视化”色彩使用 Filmic 曝光、暗部抬升与适度饱和度增强；`C` 键可切换低曝光物理观察。该分级只作用于显示，不改变星历状态。
- 火星、土星、海王星分别建立红褐、浅金、深钴蓝程序化色域；纯黑天空改为低亮度深海军蓝，土星环改为带卡西尼缝的程序化薄环。

新增 `voyager_test_runner.gd`，验证官方星历加载、地球起点距离、木星飞掠距离、时间拖动条与快捷键同步、事件跳转、太阳系总览、83,337 颗星表实例、显示分级和行星色域，以及默认场景只保留紧凑时间控制层。

## 最终架构

```text
                         libphyz (C++20 / double)
                      authoritative physics state
                     /            |               \
        native Win32 lab     Godot GDExtension     tests/examples
        scientific tools     thin data adapter     CTest/regression
                                  |
                         Godot mission/UI/rendering
```

游戏规则和渲染不再改写天体位置。场景只定义初始条件、力模型、任务目标与预算；之后的位置和速度均由同一套 `libphyz` 数值积分得到。

## 阶段验收

| 阶段 | 交付 | 验收结果 |
|---|---|---|
| L0 | 原生行为基线与无窗口测试 | 通过 |
| L1-L3 | 状态/单位、力管线、Leapfrog-2、Yoshida-4 | 通过解析解与阶数收敛测试 |
| L4 | 守恒量、轨道根数、物理事件、轨迹分支、版本快照 | 通过确定性与事件测试 |
| L5 | 原生实验室调用 `libphyz` 算法 | 原有三组回归全部通过 |
| G0-G2 | Godot 4.6.3、godot-cpp、GDExtension、3D 垂直样例 | 编辑器加载与主场景无头运行通过 |
| G3 | 五关任务、delta-v、未来机动节点、预测、评分、存档 | Godot 自动集成测试通过 |
| G4 | debug/release 扩展、构建脚本、Windows 导出 | 导出包无头启动通过 |

## `libphyz` 首版能力

- 显式 AU/太阳质量/年与 SI 单位描述，稳定 `BodyId`，物理状态和显示元数据分离。
- 可组合 Force Pipeline；牛顿、Plummer 软化、Paczynski-Wiita 成对力与常加速度模型。
- 二阶 Leapfrog 和四阶 Yoshida 固定步长辛积分器；公开算法特征、步进状态和力评估次数。
- 机械能、线动量、角动量、质心、动量残差；非保守系统不会伪报机械能守恒。
- 椭圆/抛物/双曲圆锥曲线的瞬时轨道根数。
- 碰撞、近距离交会、事件视界与洛希极限检测。
- 不修改源状态的轨迹分支预测，可在任意未来时刻加入脉冲机动。
- 保存单位、状态、力模型和积分器的版本化确定性快照；Godot 包装层额外保存任务预算和待执行节点。
- CMake `install()` 和 `find_package(libphyz)` 配置、示例程序与自动测试。

## 独立游戏垂直切片：《余烬航线》

Godot 前端已经从算法调试面板升级为可以直接游玩的单人战役。三章五关围绕“太阳系边缘最后一名引力操作员”展开：

| 章节 | 关卡 | 物理主题 | 自动化验收解 |
|---|---|---|---|
| 火种离港 | 离港窗口 | 轨道转移 | `Δv=(-1.50, 0.60, 0)` |
| 火种离港 | 静默信标 | 交会对接 | `Δv=(-0.80, 0.16, 0)` |
| 巨人的回声 | 借光而行 | 引力弹弓/出口速度保持 | 战役内导航建议 |
| 巨人的回声 | 苍穹偏转 | 小行星偏转 | `Δv=(-0.30, -0.18, 0)` |
| 三阳尽头 | 最后的观测者 | 混沌系统生存 | 零脉冲基线 |

玩家输入 delta-v 和绝对节点时刻，黄色轨迹显示独立预测分支；提交后，求解器先精确推进到节点、施加脉冲，再继续积分。任务成功/失败和分数只由实际状态、时间与燃料消耗计算。

### 可玩性与界面改造

- HUD 重制为顶部航线栏、左上目标卡和底部机动控制台，轨道重新成为绝对主画面。
- 专业遥测、轨道根数和存档功能默认折叠，需要时才展开。
- 飞船重建为由承力舱、太阳翼、散热器、天线、RCS 和电推进器组成的程序化 3D 航天器，并实时对齐速度矢量。
- 任务简报由文字弹窗升级为电影化章节画面；背景星空、行星边缘光和轨道线全部由 Godot shader 实时绘制，不使用生成式贴图。
- 飞船、任务目标和主天体使用不同颜色与角色标签；所有放大均仅影响显示，物理半径没有改变。
- 修正物理 XY 平面到 Godot XZ 世界的坐标映射，轨道不再以错误视角侧立。
- 增加目标连线、黄色预测轨迹、星空和参考网格，镜头可旋转、缩放并聚焦飞船/目标/全系统。
- 增加首次任务教程、导航建议、剧情简报、成功/失败复盘、逐关解锁、最佳分数和本地战役存档。
- 调整第三关的成功判据，使目标从不可达的增速阈值变为有限预算下可验证的出口速度保持。

### 自行验收

- `test_runner.gd`：验证 3 章 5 关内容完整，并逐关执行导航建议直到得到成功结果。
- `ui_smoke_runner.gd`：实例化真实驾驶舱，完成“关闭简报 → 导航建议 → 提交节点 → 运行 → 成功结算 → 解锁下一关”的完整首关路径。
- 视觉结构检查：目标卡尺寸受限、专业数据默认折叠、程序化背景 shader 成功载入、玩家飞船包含至少 12 个可见功能部件。
- Windows 导出包：发布构建后以 `--headless --quit-after 10` 启动验证。
- 人工视觉检查：1440×900 实际 GUI 截图确认面板无溢出、主体可辨、轨迹和标签清晰。

## 构建与运行

```powershell
.\scripts\bootstrap_godot.ps1
.\scripts\build_all.ps1 -GodotTarget template_release
.\scripts\run_game.ps1
```

单独导出 Windows 包：

```powershell
.\scripts\export_game.ps1
```

输出是 `dist/PhyzBox.exe`、`dist/PhyzBox.pck` 和对应 release GDExtension DLL。bootstrap 脚本使用 HTTP Range 从 Godot 的多平台模板包中只提取 Windows 模板，避免下载整套约 1.25 GB 的无关平台文件。

## 明确边界

本里程碑没有把 Barnes-Hut/FMM、GPU、SPH、完整广义相对论、刚体姿态、连续推力最优控制或多人网络伪装成已经实现。它们属于后续独立版本；当前交付重点是可信、可组合、可验证的直接 N-body 核心，以及一个完整可玩的轨道工程闭环。
