# PhyzBox 开发日志

更新时间：2026-06-20

这份日志记录 PhyzBox 从“三体物理演示器”逐步扩展到“程序化宇宙探索原型”的主要开发过程、关键技术取舍、遇到的疑难 bug 和暂时没有做完的风险点。

## 1. 初始目标

最初目标是用 C++ 写一个 3D 物理引擎，能够实时模拟类似《三体》气质的三体运动，并在宇宙空间里有可视化演示。

项目从空工作区起步，最终采用：

- C++20
- Win32 窗口
- 传统 OpenGL 即时模式渲染
- 无外部依赖
- MinGW `g++` 构建

这个选择的好处是项目轻、可直接编译运行；缺点是没有现代引擎的材质、地形、输入系统、资源管理和大世界坐标体系。

## 2. 三体模拟阶段

第一版做了三体场景、星空背景、轨迹、HUD 和鼠标相机。最初看起来过于稳定，像三个天体在规整轨道上跳舞，不像用户想要的《三体》式混沌。

后续调整：

- 默认场景改成非共面、非闭合的混沌三体。
- 加入 figure-eight、倾斜拉格朗日三角、层级双星等对照场景。
- 引力单位改成天文单位制：AU、太阳质量、年。
- 引力常数使用 `G = 4π²`。
- 积分器升级为四阶 Yoshida 辛积分。
- 近距离交会时自动细分步长。
- HUD 显示能量漂移、角动量漂移、最近距离和系统状态。

主要难点：

- 三体问题的“无解”不是不能数值模拟，而是没有通用闭式解析解；必须强调轨道是实时积分得到的，不是预制动画。
- 如果初值太规整，会显得稳定而缺少混沌美感。
- 如果初值太激进，近距离交会会导致数值爆炸，需要软化项和自适应子步长。

## 3. 时间控制和交互阶段

用户觉得模拟速度太快，于是加入：

- 自动时间倍率：远距离时稍快，近距离飞掠时自动变慢。
- `Base speed` 滑条：连续调节基准时间倍率。
- `+` / `-` 快捷键。
- `A` 开关自动时间倍率。

主要难点：

- 物理步长和视觉播放速度不能混为一谈。步长太大会破坏守恒，播放倍率太慢又不好看。
- 滑条区域和 HUD 文字区域必须对齐，否则鼠标拖动会误触相机旋转。

## 4. 初始条件配置阶段

后来加入 `phyzbox.ini`，支持用户配置初始天体数量、位置、速度、质量、颜色等。

支持字段包括：

- `body_count`
- `name`
- `type`
- `mass`
- `radius`
- `physical_radius`
- `density`
- `temperature`
- `luminosity`
- `spin_axis`
- `rotation_period`
- `rotation_angle`
- `color`
- `position`
- `velocity`

主要难点：

- 如果没有配置文件，必须回退到默认场景。
- 如果配置文件只写了一部分字段，缺失字段要自动补合理默认值。
- 配置里的 `radius` 是可视半径，`physical_radius` 是碰撞、洛希极限等物理判定用半径，这两个概念容易混淆。
- 后来加入飞行器后，`physical_radius` 的下限需要放低，否则小尺度飞船会被解析器强行放大。

## 5. 天体类型、黑洞和洛希极限阶段

新增天体类型系统：

- `star`
- `planet`
- `black_hole`
- `neutron_star`
- `white_dwarf`
- `minor_body`
- `spacecraft`

新增物理/视觉效果：

- 默认半径、密度、温度、亮度估算。
- 黑洞史瓦西半径。
- 事件视界捕获。
- 吸积盘视觉。
- Paczynski-Wiita 伪牛顿强场近似。
- 洛希极限估算。
- 潮汐撕裂事件。
- 碎片测试粒子流。
- 碰撞合并和动量守恒。

主要难点：

- 用户提出“完整广义相对论”。这里没有实现完整数值 GR，因为完整 GR 需要求解动态时空度规和爱因斯坦场方程，远超当前实时 OpenGL 原型范围。实际采用的是适合实时交互的强场近似。
- 黑洞/潮汐/合并会改变系统总质量和粒子数量，所以能量守恒自检不能用普通保守系统的标准判断。
- 事件型配置的自检逻辑必须放宽：发生吞噬、撕裂或合并时，能量漂移大不等于积分器坏了。

## 6. 引力弹弓和飞行器阶段

用户要求飞船/火箭通过引力弹弓加速。新增：

- `spacecraft` 类型。
- `presets/gravity-assist.ini`。
- 内置 `5` 号引力弹弓场景。
- 飞行器速度、速度增益、最近飞掠距离 HUD。
- 飞行器小火箭视觉和尾焰。

物理模型：

- 飞行器作为低质量真实受力体参与积分。
- 不手动给飞船“动画加速”。
- 速度变化来自运动行星的引力飞掠。

主要难点：

- 初始位置和速度需要调参，太远则弹弓效果不明显，太近则容易撞上或数值变硬。
- 飞船质量必须足够小，避免反向拖动大天体。
- 飞船不应触发洛希撕裂逻辑，否则会被当作小天体错误撕裂。
- HUD 中的“速度增益”需要相对初始速度计算，重置/编辑后要重新基准化。

## 7. 程序化宇宙探索阶段

用户提出类似《无人深空》的目标：自动生成几百个星球，玩家驾驶飞船在宇宙遨游，并受到物理影响。

当前实现的是第一版太空飞行垂直切片：

- 按 `6` 进入程序化宇宙探索模式。
- 固定 seed 生成一颗主恒星、约 220 个星球和玩家飞船。
- 星球按解析轨道公转、自转。
- 飞船可以自主推进。
- 飞船同时受到主恒星和全部星球引力的综合影响。
- HUD 显示最近星球、综合引力、逃逸速度和飞船速度。
- 靠近星球时有重力井/近地擦掠事件。
- 近地碰撞做了简单表面推出和速度阻尼，避免直接穿星球。

飞船加速度模型：

```text
a_ship = a_engine + Σ GM_i r_i / |r_i|^3
```

其中 `a_engine` 来自玩家推进器，求和项来自主恒星和全部星球。HUD 里的 `net gravity` 表示这个综合引力加速度的大小。

主要难点：

- 几百个星球如果全部互相做 N 体积分，会变成 O(n²) 成本，而且轨道会很快被扰乱，不适合第一版游戏原型。
- 当前做法是星球使用解析轨道，飞船接受所有天体引力。这样保留驾驶手感和重力影响，同时性能稳定。
- 大尺度宇宙会遇到浮点精度问题；当前范围还可以，后续真正扩展大世界需要 Floating Origin 或双精度宇宙坐标 + 局部渲染坐标。
- `E` 原本是编辑模式快捷键，但探索模式里 `Q/E` 用于上下推进，因此探索模式禁用了 `E` 编辑切换。
- 几百个星球如果都绘制高精度球体和自转经纬线会影响帧率，因此探索模式里降低了球体细分，并只给主恒星/较大天体保留明显自转引导线。

## 8. 文档和编码问题

开发过程中遇到过 README 在普通 PowerShell 输出里显示乱码的问题。文件本身是 UTF-8 中文，读取时需要使用：

```powershell
Get-Content -Encoding UTF8 README.md
```

文档现在包括：

- README：运行、按键、配置、物理功能说明。
- DEVELOPMENT_LOG：开发过程、难点、bug 和技术取舍。
- `phyzbox.ini.example`：自定义初始条件示例。
- `presets/`：预设场景。

## 9. 构建和测试问题

遇到过的构建/测试问题：

- `cmd /c build.bat` 在某次调用里没有找到脚本，改成显式 `cmd /c .\build.bat` 后正常。
- 曾经把构建和运行自检并行执行，导致自检可能跑到旧版 exe。后续依赖构建结果的测试必须顺序执行。
- GUI 程序无法在无窗口自检里验证视觉效果，只能通过 CLI 自检验证物理和配置，再人工启动窗口观察画面。

当前常用验证命令：

```powershell
cmd /c .\build.bat
.\bin\PhyzBox.exe --self-test
.\bin\PhyzBox.exe --self-test-explorer
.\bin\PhyzBox.exe --self-test-config .\presets\gravity-assist.ini
.\bin\PhyzBox.exe --self-test-config .\phyzbox.ini.example
```

## 10. 已知限制

当前还没有实现：

- 真正的星球地表程序化地形分块。
- 无缝大气进入和地表降落。
- 飞船姿态刚体动力学。
- 燃料、发动机热量、电力系统。
- 资源采集、基地、生态、生物、任务系统。
- 星球间真实多体扰动。
- 完整数值广义相对论。
- 大世界 Floating Origin。

这些不是 bug，而是后续阶段的开发方向。

## 11. 推荐下一步

最自然的下一步是做“星球近地层”：

- 当飞船靠近某个星球时，切换到以该星球为局部原点的坐标系。
- 根据星球 seed 生成局部地形块。
- 加入高度图、山脉、峡谷、冰原、海洋或荒漠。
- 增加大气层颜色和阻力。
- 让飞船可以降落，而不只是擦掠表面。

等近地层稳定后，再考虑地表行走、资源、基地和生态。
