# PhyzBox 物理引擎化改造报告

日期：2026-08-04
状态：方案已获批准并完成实施；代码仍只在本地工作区，**未暂存、未提交**。

## 1. 为什么当前项目显得像预设游戏

审计后发现，普通三体/多体场景确实使用实时 N-body 积分，但 `6` 号“程序化宇宙”走了另一套特殊规则：

1. 约 220 颗星球并不参加 N-body 演化；程序每帧用解析圆轨道公式直接覆盖它们的位置和速度。
2. 只有玩家飞船读取全部天体的引力，星球不会因其他星球或飞船发生扰动、进动、散射和交换能量。
3. 飞船带有真空中的指数阻尼、硬速度上限、按显示球体半径反弹和位置推出规则。
4. 显示半径被用于部分接近/地表判断，视觉表现与物理尺度没有完全隔离。
5. HUD 在探索场景中隐藏了能量、角动量等守恒诊断，更强调“驾驶状态”。

因此，这个场景的运动结果主要由场景代码预先规定，而不是统一的动力学方程自然产生。这正是“像游戏、不像物理引擎”的主要来源。

## 2. 对照的成熟引擎/轨道动力学框架

### REBOUND / REBOUNDx

- REBOUND 把系统表示为粒子状态，由可选择的积分器推进；WHFast 面向中心天体主导的长期轨道，IAS15 适合高精度和近距离交会。
- REBOUNDx 把广义相对论、潮汐、辐射等额外物理作为可组合效应加入，而不是写成特定场景动画。
- 参考：[REBOUND](https://rebound.hanno-rein.de/)、[WHFast 说明](https://rebound.hanno-rein.de/ipython_examples/WHFast/)、[REBOUNDx](https://reboundx.readthedocs.io/en/latest/)

### Orekit

- 数值传播器负责积分状态，ForceModel 只负责提供加速度；事件检测与传播过程分离。
- 参考：[Orekit NumericalPropagator](https://www.orekit.org/static/apidocs/org/orekit/propagation/numerical/NumericalPropagator.html)、[Orekit propagation architecture](https://www.orekit.org/site-orekit-6.1/architecture/propagation.html)

### NASA GMAT

- Propagator 由数值积分器和 ForceModel 组合，轨道不是由演示场景逐帧写死；有动力飞行则在力模型中加入推力项。
- 参考：[GMAT Propagator](https://documentation.help/GMAT/Propagator.html)

这些项目的共同点不是“功能更多”，而是：**初始条件只定义起点，状态由统一方程推进；力、积分、事件和诊断彼此职责清楚。**

## 3. 建议的改造目标

本轮不追求一次性做成科研级软件，而是先消除最明显的预设游戏逻辑：

1. 所有有质量天体进入统一的成对引力计算。
2. 场景生成器只生成初始状态，不再控制后续轨迹。
3. 飞船操作作为有限外力加入，不使用真空阻尼、速度上限或位置吸附。
4. 显示半径和物理半径严格分离。
5. HUD 与无窗口测试公开积分器和守恒误差。
6. 修复已有力模型中破坏线动量守恒的问题。

## 4. 当前工作区已经试改的内容

以下内容已经写入本地文件，但还没有 Git 暂存或提交。

### A. 统一 `6` 号场景与 N-body 核心

- 删除星球的 `proceduralOrbit`、解析轨道相位/角速度等运行时字段。
- 删除每帧 `updateProceduralOrbits()` 位置覆盖。
- 恒星、32 个行星/小天体和飞船共 34 个活动体全部由四阶 Yoshida 积分器推进。
- 32 个轨道体使用对数半长轴、小偏心率、小倾角和较真实的质量范围生成初始状态。
- 从 220 个预设轨道体缩减到 32 个实时 N-body 轨道体，避免用不合理的行星密度换视觉数量，也控制直接求解的 O(n²) 成本。

涉及文件：`src/NBodySystem.hpp`、`src/NBodySystem.cpp`

### B. 删除游戏式飞船规则

- 删除指数阻尼刹车。
- 删除 72/120 AU/yr 的硬速度上限。
- 删除按放大显示球体推出飞船、反弹和速度衰减。
- `Ctrl` 改为逆速度方向的有限反推；普通推进与加力推进仍是明确的外加速度。
- 推力使用对称的半步 kick 包围引力积分，避免把位置直接改写成“驾驶结果”。

涉及文件：`src/NBodySystem.cpp`、`README.md`

### C. 修复强场成对力的守恒问题

原实现中，黑洞吸引普通天体时使用 Paczynski-Wiita 近似，而普通天体对黑洞的反作用使用普通牛顿力，两边不相等，会破坏系统线动量。

当前试改为：

- 每一对天体先计算同一个成对力尺度。
- 对双方施加等大反向的力，分别除以各自质量得到加速度。
- 总能量中的黑洞势能改为与实际 Paczynski-Wiita 力一致的势函数。

涉及文件：`src/NBodySystem.cpp`

### D. 分离显示尺度与物理尺度

- 程序生成行星不再用 `visualRadius * 0.22` 作为碰撞半径。
- 修正低质量行星和小天体的默认物理半径缩放，避免所有低质量天体被钳制到木星级质量再估算半径。
- 飞船接近事件使用真实接触距离和行星 Hill 球。
- 逃逸速度使用真实物理半径作为距离下限。

涉及文件：`src/NBodySystem.cpp`

### E. 增强诊断，而不是增强“玩法提示”

- HUD 显示 `Yoshida-4 symplectic`、直接 N-body 活动体数量和时间步。
- 所有场景显示能量漂移、角动量漂移和线动量残差。
- 新增 `totalLinearMomentum()` 与 `linearMomentumError()`。
- 飞船行显示速度、综合引力和最近行星距离。

涉及文件：`src/NBodySystem.hpp`、`src/NBodySystem.cpp`、`src/Renderer.cpp`

### F. 增强无窗口验收

- 原五个场景增加线动量残差阈值。
- 新行星系统先在无推力状态运行，检查：
  - 所有非飞船活动体都有真实动力学加速度；
  - 能量漂移小于阈值；
  - 线动量残差小于阈值。
- 然后再施加有限推力，检查飞船状态是否保持有限且可用。

涉及文件：`src/main.cpp`

## 5. 当前验证结果

已经完成一次本地构建和无窗口测试：

```text
build.ps1                                      通过
PhyzBox.exe --self-test                       通过
PhyzBox.exe --self-test-explorer              通过
PhyzBox.exe --self-test-config black-hole...  通过
PhyzBox.exe --self-test-config gravity...     通过
```

关键数值：

- 原五个场景最大能量漂移：约 `4.67e-05%`。
- 原五个场景线动量残差：约 `1e-14`。
- 新 34 体系统无推力阶段最大能量漂移：约 `1.94e-11%`。
- 新 34 体系统线动量残差：约 `1.10e-15`。
- 33 个非飞船天体全部检测到非零实时引力加速度。

## 6. 风险和仍未解决的问题

1. 直接 N-body 是 O(n²)，当前适合几十个活动体；数百至数千天体应增加 Barnes-Hut、FMM 或混合辛积分器。
2. 当前自适应细分与四阶辛积分的组合是工程折中；严格意义上，状态相关变步长会影响辛结构。后续可增加固定步长模式，或引入类似 IAS15 的非辛自适应积分器用于近距离交会。
3. Paczynski-Wiita 仍是伪牛顿近似，不是完整广义相对论。
4. 碰撞目前只做完全非弹性合并，潮汐碎片是无质量测试粒子，不是流体或 SPH。
5. 飞船没有燃料质量、姿态、发动机方向/节流动态；目前只有外加速度。
6. 尚未进行 GUI 人工视觉验收；无窗口测试只验证动力学和数值稳定性。

## 7. 建议你确认的决策

在继续之前，建议确认以下方向：

1. 是否接受把 `6` 号场景从“220 个预设星球”改为“32 个实时 N-body 天体”。
2. 是否保留飞船键盘推力，还是把 `6` 号场景进一步改成纯观察/实验场景。
3. 是否保留 Paczynski-Wiita 强场近似，还是本轮先限定为纯牛顿 N-body。
4. 下一阶段优先做哪一项：
   - 可切换积分器（固定步长 Yoshida / 近交会自适应积分器）；
   - Barnes-Hut 大 N；
   - 轨道根数、质心系、Jacobi 常数等分析工具；
   - 通用可组合 ForceModel 接口。

## 8. 当前 Git 状态

- 所有变更都在工作区。
- 没有执行 `git add`。
- 没有创建 commit。
- 没有推送远端。

你确认报告后，可以选择：保留当前方向继续完善、只保留其中一部分，或撤销这批试改后按新的方案重做。
